[project]
name = "croviq-domain"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic[email]>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
